{
  "name": "brain",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive 3D model of my own brain, built from a 2023 MRI",
  "scripts": {
    "serve": "node scripts/serve.js",
    "bench": "node scripts/bench.js http://localhost:8081 build/bench.json",
    "stamp": "node scripts/stamp.js",
    "deploy": "node scripts/stamp.js && wrangler pages deploy web --project-name=brain --commit-dirty=true"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "three": "0.180.0",
    "three-mesh-bvh": "^0.9.13"
  },
  "devDependencies": {
    "puppeteer": "^24.0.0",
    "wrangler": "^4.114.0"
  }
}
